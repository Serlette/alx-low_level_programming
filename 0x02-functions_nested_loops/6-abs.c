#include "main.h"

/**
 * _abs - prints absolute value of an integer
 * @int: type of argument
 * Return: Always 0
 */

int _abs(int)
{
	int n;

	if (n < 0)
		return (-n);
	else
		return (n);

	return (0);
}
