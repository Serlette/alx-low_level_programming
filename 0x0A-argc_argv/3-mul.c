#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 *
 *
 *
 *
 */

int main(int argc, char *argv[])
{
	int i, mul;

	if (argc == 3)
	{
		for (i = 1; i < argc; i++)
		{
			mul = atoi[char argv[i];
			mul = mul * atoi(char argv[i]);
				   }
		}
}
