#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * main - function name
 * argc: argument count
 * argv: argument variable
 * Return: Always 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	int i = 0;

	printf("%s/n", argv[i]);

	return (0);
}
