#include <stdlib.h>
#include <stdio.h>

/**
 * main - function name
 * argc: argument count
 *argv: argument variable
 * Return: Always 0
 */

int main(int argc, char argv[])
{
	int i;

	if (i == 0)
		printf("%s/n", argv[i]);
	return (0);
}
