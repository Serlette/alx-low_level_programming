#include "main.h"

/**
 * *_strcat - concatenates two strings
 * @dest: first string
 * @src: second string
 * Return: a pointer
 */

char *_strcat(char *dest, char *src)
{
	int n;
	int m;
	int p;

	n = 0;
	m = 0;

	while (dest[n] != '\0')
		n++;

	while (src[m] != m && m < p)
	{
		dest[n] = src[m];
		i++;
		j++;
	}
	return (dest);
}
